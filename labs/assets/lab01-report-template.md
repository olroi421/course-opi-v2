# Звіт з лабораторної роботи 01

## Основи Python: обробка та аналіз даних

- **Виконав(ла):** ПІБ студента
- **Група:**
- **Варіант завдання (№):**
- **Тема варіанта:**
- **Виконаний рівень складності:** 1 / 2 / 3

---

## 1. Опис вхідних даних

Коротко описати структуру CSV-файлу з вхідними даними: перелік полів (стовпців), джерело даних (підготовлено вручну / знайдено відкриті дані — вказати посилання), кількість записів.

```
приклад кількох рядків з вхідного CSV-файлу
```

## 2. Пройдений онлайн-курс

- Назва курсу:
- Посилання:
- Розділи/теми, які було опрацьовано:

## 3. Опис виконаної роботи

### 3.1 Реалізовані функції (рівень 1)

Короткий опис призначення кожної реалізованої функції (2-4 речення на функцію: що приймає, що обчислює/повертає).

### 3.2 Сортування та категоризація (рівень 2, якщо виконано)

Опис реалізованого сортування та логіки розподілу за категоріями.

### 3.3 Модуль та валідація даних (рівень 3, якщо виконано)

Опис структури модуля (`data_analyzer.py` або власна назва), перелік функцій у ньому, опис реалізованої валідації некоректних/відсутніх даних, опис додаткової функції аналізу.

## 4. Вихідний код програми

Повний код програми наводиться нижче (а не завантажується окремим файлом) — під час захисту роботи це саме той код, який запускався.

### 4.1 Головний файл (`main.py`)

```python
# вставити повний вміст main.py
```

### 4.2 Модуль обробки даних (рівень 3, якщо виконано)

```python
# вставити повний вміст модуля, наприклад data_analyzer.py
```

## 5. Результати роботи програми

Вставити приклад виведеного звіту в консоль (текстом або скріншотом) та, за наявності, приклад збереженого вихідного файлу (CSV/JSON).

```
приклад виводу програми
```

## 6. Труднощі та їх вирішення

Що викликало складнощі під час виконання роботи і як це було вирішено.

## 7. Висновки

Короткий висновок про отримані знання та навички (2-4 речення).
